function guessNumberGame() {
    // Генерируем случайное число от 1 до 100
    let randomNumber = Math.floor(Math.random() * 100) + 1;
    let attempts = 0;
    let userGuess;
    
    console.log("Игра 'Угадай число' началась! Загадано число от 1 до 100.");
    
    while (true) {
        userGuess = prompt("Введите число от 1 до 100:");
        
        // Проверяем, нажал ли пользователь "Отмена"
        if (userGuess === null) {
            alert("Игра прервана. Возвращаемся на главную страницу...");
            window.location.href = "index.html"; 
            return; 
        }
        
        userGuess = parseInt(userGuess);
        attempts++;
        
        // Проверка валидности ввода
        if (isNaN(userGuess) || userGuess < 1 || userGuess > 100) {
            alert("Пожалуйста, введите корректное число от 1 до 100!");
            attempts--; // Не считаем неверные попытки
            continue;
        }
        
        if (userGuess === randomNumber) {
            let message = `🎉 Поздравляю! Вы угадали число ${randomNumber}!`;
            message += `\nКоличество попыток: ${attempts}`;
            
            if (attempts <= 5) {
                message += "\n 🏆 Отличный результат! Вы настоящий угадыватель!";
            } else if (attempts <= 10) {
                message += "\n 💥 Хороший результат!";
            } else {
                message += "\n Можно было и лучше, но главное - победа!";
            }
            
            alert(message);
            break;
            
        } else if (userGuess < randomNumber) {
            alert(`❌ Число ${userGuess} МЕНЬШЕ загаданного. Попробуйте еще раз!`);
        } else {
            alert(`❌ Число ${userGuess} БОЛЬШЕ загаданного. Попробуйте еще раз!`);
        }
    }
}

guessNumberGame();